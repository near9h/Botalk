# Inlined into the browser bundle at build time.
# Leave empty (default) so the browser uses same-origin requests via the
# nginx reverse proxy. Override only if you want the browser to bypass the
# proxy and call the backend directly (e.g. for local dev with split ports).
NEXT_PUBLIC_API_BASE=