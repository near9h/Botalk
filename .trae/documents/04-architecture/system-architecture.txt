┌──────────────────────────────────────────────┐
                              │         公网用户浏览器                  │
                              │ Next.js 14.2.15 / React 18.3.1        │
                              │ + EventSource                          │
                              └───────────────────────┬─────────────────┘
                                                      │ HTTPS :3500
                                                      ▼
                              ┌──────────────────────────────────────────┐
                              │       Nginx 1.27.5 (alpine)              │
                              │          botgroup-nginx                 │
                              │  ┌─ stream ───────────────────────────┐ │
                              │  │ :3500 → 嗅探 0x16 → TLS 终结      │ │
                              │  └───────────────────────────────────┘ │
                              │  ┌─ http ─────────────────────────────┐ │
                              │  │ :3501 → proxy_protocol → frontend  │ │
                              │  │ :3500/tls → proxy_protocol → backend│ │
                              │  └───────────────────────────────────┘ │
                              └───────────┬─────────────────┬───────────┘
                                          │ /               │ /api/*
                                          ▼                 ▼
                          ┌────────────────────────┐ ┌──────────────────────────┐
                          │  Frontend · Next.js    │ │  Backend · FastAPI        │
                          │  14.2.15               │ │  0.141.1                 │
                          │  node 20.20.2          │ │  Python 3.11.16          │
                          │  :3000 (内)            │ │  :8000 (内)              │
                          │  App Router            │ │  api/*                   │
                          │  components            │ │  services/*              │
                          │  lib/api.ts            │ │  orchestrator/           │
                          │                        │ │  workers/                │
                          │                        │ │  + LibreOffice 25.2.3.2  │
                          └────────────────────────┘ └──────┬───────────────────┘
                                                          │
              ┌──────────────────┬──────────────────┬─────┴──────┬─────────────┐
              ▼                  ▼                  ▼            ▼             ▼
   ┌─────────────────┐ ┌─────────────────┐ ┌────────────────┐ ┌──────────────────────┐
   │ Postgres 16.15  │ │ NewAPI          │ │ MinerU Cloud   │ │ 智谱 GLM API         │
   │ + pgvector 0.8.6│ │ calciumion/     │ │ API v4         │ │ embedding-3 (2048d)  │
   │ :5432           │ │ new-api:latest  │ │ mineru.net/    │ │ + rerank             │
   │ 持久化 + 向量   │ │ :5000           │ │ api/v4         │ │ open.bigmodel.cn     │
   │                 │ │ LLM 网关        │ │ PDF 解析       │ │                      │
   └─────────────────┘ └────────┬────────┘ └────────────────┘ └──────────────────────┘
                                │
                                ▼
                    ┌──────────────────────┐
                    │ 上游模型             │
                    │ gpt-4o / claude-3.5  │
                    │ gemini-2.5 / agnes / │
                    │ MiniMax-M3           │
                    └──────────────────────┘

> 这是 .trae/documents/04-architecture/system-architecture.md 的
> ASCII 备份（带版本号版本）；主流 Markdown 渲染器
> （GitHub / GitLab / VSCode / Trae IDE）都支持 Mermaid，
> 看主文档的可视化版本。